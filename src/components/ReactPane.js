import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const ReactPane = () => {
    return (
        <View>
            <Text>React Pane</Text>
        </View>
    )
};

const styles = StyleSheet.create({});

export default ReactPane;